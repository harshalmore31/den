{
  "name": "@harshalmore31/den",
  "version": "0.1.0",
  "description": "Den CLI — The living runtime for AI agents",
  "type": "module",
  "bin": {
    "den": "./dist/cli.mjs"
  },
  "scripts": {
    "dev": "bun run src/index.tsx",
    "build": "bun build src/index.tsx --outfile dist/cli.mjs --target node --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "ink": "^5.2.0",
    "react": "^18.3.1",
    "react-devtools-core": "^7.0.1",
    "yaml": "^2.8.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=18"
  }
}
